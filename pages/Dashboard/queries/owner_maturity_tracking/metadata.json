{
  "gitSyncId": "6829a14f0b957058348e8af4_ee07bf1c-bbd3-4a19-9bf6-27b5d3a25dd3",
  "id": "Dashboard_owner_maturity_tracking",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT \n    batch_id,\n    batch_name,\n    product_owner,\n    maturity_date,\n    DATEDIFF(maturity_date, CURDATE()) AS days_until_expiry,\n    'Expiring in more than 2 Months' AS category\nFROM \n    batch\nWHERE \n    maturity_date > CURDATE() + INTERVAL 2 MONTH\n    AND product_owner = (SELECT name FROM account WHERE user_id = '{{appsmith.store.user_id}}')\n\nUNION ALL\n\nSELECT \n    batch_id,\n    batch_name,\n    product_owner,\n    maturity_date,\n    DATEDIFF(maturity_date, CURDATE()) AS days_until_expiry,\n    'Expiring in 10 Days' AS category\nFROM \n    batch\nWHERE \n    maturity_date BETWEEN CURDATE() AND CURDATE() + INTERVAL 10 DAY\n    AND product_owner = (SELECT name FROM account WHERE user_id = '{{appsmith.store.user_id}}')\n\nUNION ALL\n\nSELECT \n    batch_id,\n    batch_name,\n    product_owner,\n    maturity_date,\n    DATEDIFF(maturity_date, CURDATE()) AS days_until_expiry,\n    'Expiring in 2 Months' AS category\nFROM \n    batch\nWHERE \n    maturity_date > CURDATE() + INTERVAL 10 DAY\n    AND maturity_date <= CURDATE() + INTERVAL 2 MONTH\n    AND product_owner = (SELECT name FROM account WHERE user_id = '{{appsmith.store.user_id}}')\n\nORDER BY \n    days_until_expiry ASC;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "SE database",
      "isAutoGenerated": false,
      "name": "SE database",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "executeOnLoad": true,
    "name": "owner_maturity_tracking",
    "pageId": "Dashboard",
    "userSetOnLoad": false
  }
}